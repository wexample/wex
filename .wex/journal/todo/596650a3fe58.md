# Tests filestate restants (13 échecs) — causes diagnostiquées, non corrigées

Opened: 2026-08-27
Updated: 2026-08-27
Author: agent:main

Après le fix des 7 tests `_original_uid`/`_original_*` (champs `attrs` sans `default=None`, donc jamais initialisés — corrigé), il reste 13 tests filestate en échec. Les causes ont été diagnostiquées pendant la session mais aucun fix n'a été appliqué :

| Famille | Nb | Cause probable |
|---|---|---|
| `test_name_*` (chaîne `apply`) | 7 | À creuser — possiblement encore un `private_field` sans `default=` manquant, mais sur un autre fichier que celui déjà corrigé |
| `test_on_bad_format` | 3 | Probable bug fonctionnel dans la résolution de `bad_format` |
| `test_xml_file::test_file_dumps` | 1 | `xmltodict` lève une erreur de parsing expat — bug dans le XML dumper |
| `test_configure_from_callback*` | 2 | La validation de type rejette les callables — pré-existant (déjà KO sur la baseline avant le refactor lazy), sujet séparé |
| `test_search_result` | 1 | Logique de match regex incorrecte — sujet séparé |

À noter : baseline (avant le passage en lazy) = 20 failed / 168 passed ; après les fixes de cette session = 13 failed / 168 passed (les 7 `_original_*` corrigés). Ces 13 restants sont indépendants du passage lazy/eager fait dans la même session.
