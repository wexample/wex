# Localiser le script de détection de dépendances circulaires pour évaluation async

Opened: 2026-08-27
Updated: 2026-08-27
Author: agent:main

L'utilisateur pense qu'il existe un script de détection des dépendances circulaires entre packages qui serait un bon candidat à la parallélisation (comme `suite/status.py`). L'agent n'a rien trouvé de dédié — seulement des classes de test (`attrs_circular_*`). Aucun chemin exact n'a été donné pendant la conversation.

À faire : demander/retrouver le chemin exact du script visé, puis évaluer s'il opère en boucle indépendante sur des packages (candidat direct à `parallel_map`, comme fait pour `suite/status.py`).
