# Roadmap async : Phase 1.1 filestate apply_operations à faire

Opened: 2026-08-28
Updated: 2026-08-28
Author: agent:main

Le roadmap async (`async.md`) a été remis à jour en session pour refléter l'état réel : plusieurs phases identifiées au départ n'ont jamais été attaquées, la session ayant dérivé vers le bootstrap (cache YAML + lazy filestate + eager opt-in, gain réel mais hors scope initial).

État des phases restantes à la fin de la session :

| Phase | Sujet | État |
|---|---|---|
| **1.1** | filestate `apply_operations` — boucle séquentielle sur les opérations de chaque item | **non traité**, la session s'arrête juste au moment où l'assistant commence à s'y attaquer |
| 1.2 | `children_filter_option` (iterdir + checks) | partiellement résolu côté bootstrap par le lazy loading |
| 1.3 | `_packages_execute` (subprocess sur N packages) | non traité (Axe A déjà partiellement couvert ailleurs) |

À faire : reprendre concrètement la Phase 1.1 — paralléliser/rendre async la boucle d'application des opérations filestate par item — puis évaluer 1.3. Voir `async.md` pour le détail du roadmap remis à jour.
