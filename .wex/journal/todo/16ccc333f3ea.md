# App init git

When running app/init, also run git init + git commit -a -m "First commit".
Keep in mind the case where app init is run on an existing directory.
Maybe "true" by default, with an argument to disable the git init.
