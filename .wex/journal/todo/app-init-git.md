Lors de app/init, faire aussi un git init + git commit -a -m "First commit".
Garder en tête le cas ou on ferais un app init sur un dossier existant
peut etre "true" par defaut avec un argument pour désactiver le git init
