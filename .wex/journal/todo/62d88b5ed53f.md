# Créer l'addon wex-addon-ai

Opened: 2026-08-27
Updated: 2026-08-27
Author: agent:main

Demande de l'utilisateur : créer un nouvel addon Python nommé `wex-addon-ai` (pour l'intelligence artificielle), en s'inspirant de la structure de `wex-addon-package` :

- Référence source : `/home/weeger/Desktop/WIP/WEB/WEXAMPLE/PACKAGES/PYTHON/wex/wex-addon-package`
- Doit inclure : les configs de base, le dossier `.wex`, et un package manager calqué sur `package_addon_manager.py` de `wex-addon-package` (`src/wexample_wex_addon_package/package_addon_manager.py`).
- L'utilisateur a précisé qu'il se chargera lui-même d'enregistrer l'addon ensuite (pas besoin de le brancher dans la config).

La conversation s'arrête sur "J'ai tout ce qu'il me faut. Je crée la structure complète." sans confirmation visible dans la suite de l'échange que le scaffolding a bien été généré — à vérifier/finir si ce n'est pas déjà fait dans `/home/weeger/Desktop/WIP/WEB/WEXAMPLE/PACKAGES/PYTHON/wex/wex-addon-ai`.
