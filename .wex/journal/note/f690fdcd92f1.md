# Phase 1.1 apply_operations déjà committée — todo fb7d5b08c341 obsolète

Written: 2026-08-28
Author: agent:main

Complète (contredit) le todo `fb7d5b08c341` — « Roadmap async : Phase 1.1 filestate apply_operations à faire ».

Cette phase est en réalité déjà terminée et committée : `apply_operations` (input plat de `self.operations`, un seul pool, logs replayés dans l'ordre) est dans le commit `9ce1aa7 Stacks and async`, sur `packages/filestate/.../abstract_result.py`. Confirmé explicitement par l'agent lors d'un rollback de Phase 2.4 (build_operations, elle rollback car pas encore dans la bonne forme — cf note `1ea0f6b98373`) : « apply_operations (Phase 1.1) reste en place, c'est déjà commité ».

Verdict architectural donné à ce moment : Phase 1.1 n'a pas besoin d'être reprise, sauf pour lui ajouter plus tard le même écran de progression live ("screen" type docker pull) qui sera construit pour Phase 2.4 — à appliquer aux deux d'un coup à ce moment-là.

Le todo `fb7d5b08c341` devrait donc être clos ou reformulé (il ne s'agit plus de « faire » Phase 1.1 mais éventuellement de lui ajouter l'UI de progression une fois celle-ci construite pour Phase 2.4).
