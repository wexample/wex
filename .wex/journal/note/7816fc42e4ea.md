# Ticket doc : état des phases après revue de roadmap

Written: 2026-08-27
Author: agent:main

Mise à jour du ticket de documentation (phase 5 / roadmap post-migration) suite à une revue de statut :

- **Phase 2** : close. Les 7 tickets restants ont été déplacés de `PACKAGES/PYTHON/.wex/knowledge/todo/` vers `.wex/journal/todo/` (en `git mv`, historique conservé), et le `project-info/` vide oublié en phase 1 a été supprimé. Le `knowledge/` de la suite Python est désormais conforme : `contributing`, `package-readme`, `readme`, `specifications`, `usage`.
- **Phase 4** : marquée complète mais reformulée — elle demandait « un document de référence unique », réponse finale apportée par le système de contrats à la place : les règles vivent à côté du fichier qu'elles gouvernent, et un agent ne reçoit que celles qui s'appliquent. Seul reliquat : le message d'erreur de `path()`.
- **Phase 5, cas A** : coché — composeur hérité, sommaire en deux passes, `addons()`, pages obligatoires en place. Un arbitrage reste en attente sur `readme.section…` (tronqué dans l'échange).

Décision de l'opérateur : les trois axes (consulter la doc, l'écrire, corriger les docs individuellement) ne sont pas hors scope les uns des autres — « je pense que rien n'est hors scope, il faut tout faire, dans un ordre ou dans un autre » — à mener en parallèle plutôt que séquentiellement.
