# rectify séquentiel : cause RLock + décision Phase 1.1

Written: 2026-08-28
Author: agent:main

Complète les notes `4fbe15a75c00` (pools imbriqués ne compensent pas en parallélisme réel) et `1ea0f6b98373` (fix flatten+un seul pool proposé, non implémenté).

Deux détails supplémentaires apparus en creusant le diagnostic de `rectify` toujours perçu comme séquentiel après Phase 2.4 :

1. **Cause du sérialisé même quand plusieurs threads sont dispatchés** : `self.log()` + `SpinnerPool.next()` dans chaque check passent par un `threading.RLock` partagé — les threads s'enfilent au moment d'écrire la ligne de progression au terminal, même si l'inspection elle-même tourne en parallèle.

2. **Décision sur Phase 1.1 (`apply_operations`)** : elle n'a pas besoin d'être reprise. Architecturalement elle a déjà la bonne forme (input plat via `self.operations`, un seul pool, logs replayés dans l'ordre). Le seul manque est le **screen UI live** (façon Docker layers) — à construire pour Phase 2.4 (build_operations) puis à appliquer aux deux en même temps, pas seulement à Phase 2.4.
