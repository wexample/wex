# Addon: docker

## v5 reference

`wex-5/addons/docker/`

## Commands

- [ ] `container/runs` — list running containers
- [ ] `docker/ip` — get Docker bridge IP
- [ ] `docker/stop_all` — stop all running containers

## v6 target

- `PACKAGES/PYTHON/wex/wex-core` addons (or dedicated `wex-addon-docker`)
