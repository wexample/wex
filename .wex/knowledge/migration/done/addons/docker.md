# Addon: docker

## v5 reference

`wex-5/addons/docker/`

## Status

- [~] `container/runs` — functionally covered enough by `app::container/list` for the current migration scope, even without a dedicated global command
- [x] `docker/ip` — global Docker local IP command restored in v6
- [x] `docker/stop_all` — global Docker cleanup command restored in v6

## Conclusion

This addon no longer blocks a functionally complete v6.
