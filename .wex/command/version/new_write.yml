type: app
help: Create new version
command:
  decorator: app_command
attach:
  after: app::version/new-write
scripts:
  - title: Save new version to root file
    script: echo $(wex app::version/get) > version.txt
