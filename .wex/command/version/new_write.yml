type: app
help: Create new version
command:
  decorator: app_command
attach:
  before: app::version/new-write
options:
  - name: --version
    short: -v
    help: New version
    required: true
    type: str
scripts:
  - title: Save new version to root file
    script: echo ${VERSION} > version.txt
