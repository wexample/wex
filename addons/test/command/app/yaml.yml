type: addon
webhook: false
help: Description
command:
  decorator: app_command
  options:
    # Do not change default but test definition
    should_run: true
decorators:
options:
  - name: --option
    short: -o
    help: This is an option
    required: true
    type: bool
scripts:
  - echo "Inline bash script example"

  - title: Bash outside container but with running app
    app_should_run: true
    script: echo "Inline bash script outside running app in ${APP_DIR}"
