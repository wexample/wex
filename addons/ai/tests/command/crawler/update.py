from addons.ai.command.crawler.update import ai__crawler__update
from tests.AbstractTestCase import AbstractTestCase


class TestAiCommandCrawlerUpdate(AbstractTestCase):
    def test_update(self):
        # Too early to test
        # self.kernel.run_function(ai__crawler__update, {
        #     'name': 'test'
        # })
        self.assertTrue(True)
