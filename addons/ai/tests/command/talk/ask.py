from addons.ai.command.talk.ask import ai__talk__ask
from tests.AbstractTestCase import AbstractTestCase


class TestAiCommandTalkAsk(AbstractTestCase):
    def test_ask(self):
        # Too early to test
        # self.kernel.run_function(ai__talk__ask, {
        #     'name': 'test'
        # })
        self.assertTrue(True)
