services:
    maria_10:
        container_name: '${RUNTIME_NAME}_maria_10'
        image: mariadb:10.9
        environment:
            - MYSQL_ROOT_HOST=%
            - MYSQL_PASSWORD=${SERVICE_MARIA_10_PASSWORD}
            - MYSQL_DATABASE=${SERVICE_MARIA_10_NAME}
            - MYSQL_ROOT_PASSWORD=${SERVICE_MARIA_10_PASSWORD}
            - MYSQL_USER=${SERVICE_MARIA_10_USER}
        volumes:
            - ${RUNTIME_PATH_APP_WEX}maria/data:/var/lib/mysql
            - ${RUNTIME_PATH_APP_WEX}maria/dumps:/var/www/dumps
            - ${RUNTIME_PATH_APP_WEX}tmp/mysql.cnf:/tmp/mysql.cnf
        extends:
            file: ${RUNTIME_SERVICE_DEFAULT_YML_ENV}
            service: default
        ports:
            - ${SERVICE_MARIA_10_PORT}:3306