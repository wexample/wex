from tests.AbstractTestCase import AbstractTestCase


class TestCoreCommandConfigureAll(AbstractTestCase):
    def test_all(self) -> None:
        # response = self.kernel.run_function(core__configure__all)
        # Do not test if only interactive
        self.assertTrue(True)
