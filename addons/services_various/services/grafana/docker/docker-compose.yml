services:
    grafana:
      container_name: ${RUNTIME_NAME}_grafana
      image: grafana/grafana:${GRAFANA_VERSION}
      volumes:
        - ${PATH_APP}grafana:/var/lib/grafana
        - ${PATH_APP}provisioning:/etc/grafana/provisioning/
      environment:
        - VIRTUAL_HOST=${CONTEXT_DOMAINS}
        - VIRTUAL_PORT=80
      extends:
          file: ${SERVICE_DEFAULT_YML_ENV}
          service: default